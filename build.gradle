plugins {
    id 'java'
}

group = 'com.zakharoval78'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(
    "org.junit:junit-bom:5.10.0",
    "org.junit.jupiter:junit-jupiter")
}

test {
    useJUnitPlatform()
}